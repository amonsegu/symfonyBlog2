{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edition catégorie</h1>

    {# ouverture de la balise form #}
    {{ form_start(form) }}

    {# champ de formulaire avec son label #}
    {{ form_row(form.name) }}

    <div class="text-right">
        <button type="submit" class="btn btn-primary">
            Enregistrer
        </button>
        <a class="btn btn-outline-secondary"
           href="{{ path('app_admin_category_index') }}">
            Retour
        </a>
    </div>

    {# fermeture de la balise form #}
    {{ form_end(form) }}
{% endblock %}

